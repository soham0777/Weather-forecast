import React from 'react'
import ReactDOM from 'react-dom/client'
import { SmartNest } from './pages/SmartNest'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SmartNest />
  </React.StrictMode>,
)
